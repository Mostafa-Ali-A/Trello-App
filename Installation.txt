npm install -g npm-check-updates

npm remove

npx create-react-app my-app
cd my-app
npm install gh-pages --save-dev
git init
git remote add origin git@github.com:mostafa-ali-a/Markdown-Previewer.get
git config --global user.email "mlovelylord@gmail.com"
git config --global user.name "mostafa-ali-a"
git config --global user.name "Markdown Previewer"

{
"homepage": "https://mostafa-ali-a.github.io/Markdown-Previewer",
"homepage": "https://github.com/Mostafa-Ali-A/Markdown-Previewer.git",
"devDependencies": {
  "gh-pages": "1.0.0",
  "react-scripts": "1.0.13"
},
}

"scripts": {
"predeploy": "npm run build",
"deploy": "gh-pages -d build",
"deploy": "gh-pages-clean gh-pages -d build",
},

npm run deploy
npm start

npm run build
npm install -g serve
serve -s build

npm test
npm run eject(
Removes this tool and copies build dependencies, configuration files
and scripts into the app directory. If you do this, you can’t go back!)

npm install @reduxjs/toolkit react-redux
npm i redux

npm install --save-dev @babel/preset-react
.babelrc file
{
  "presets": ["@babel/preset-react"]
}

var babel = require("@babel/core");
import { transform } from "@babel/core";
import * as babel from "@babel/core";

npm i -g webpack
npm i -D uglifyjs-webpack-plugin
webpack

npm install bootstrap
# OR
yarn add bootstrap

index.js
// Bootstrap CSS
import "bootstrap/dist/css/bootstrap.min.css";
// Bootstrap Bundle JS
import "bootstrap/dist/js/bootstrap.bundle.min";

npm install jquery
npm install jsdom

npm i marked
npm i prismjs
